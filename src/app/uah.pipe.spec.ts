/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { UahPipe } from './uah.pipe';

describe('UahPipe', () => {
  it('create an instance', () => {
    let pipe = new UahPipe();
    expect(pipe).toBeTruthy();
  });
});
